# URL of the deployed backend (no trailing slash).
# Leave unset in development — the Vite dev proxy handles /api automatically.
# Required for production builds.
VITE_API_URL=https://owl-fund-intelligence.onrender.com
